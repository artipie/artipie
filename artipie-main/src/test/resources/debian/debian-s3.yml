---
repo:
  type: deb
  storage:
    type: s3
    bucket: buck1
    region: s3test
    endpoint: http://minioc:9000
    credentials:
      type: basic
      accessKeyId: minioadmin
      secretAccessKey: minioadmin
  settings:
    Components: main
    Architectures: amd64
