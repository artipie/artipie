meta:
  storage:
    type: fs
    path: /var/artipie/repo
  layout: flat
  base_url: http://artipie:8080/
  credentials:
    - type: artipie
  policy:
    type: artipie
    storage:
      type: fs
      path: /var/artipie/security
