meta:
  storage:
    type: fs
    path: /var/artipie/repo
  credentials:
    type: file
    path: _credentials.yaml
    alternative_auth:
      type: github
  layout: org
  base_url: http://central.artipie.com/
  metrics:
    type: log
    interval: 60
